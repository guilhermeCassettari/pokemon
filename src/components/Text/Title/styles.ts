import styled from "styled-components/native";

export const Title = styled.Text`
    font-weight: bold;
    font-size: 18px;
    text-transform: capitalize;
    letter-spacing: 0px;
    color: #FFFFFF;
    opacity: 1;
`